version: "3.3"

services:
  myApp:
    image: soul0silver/reactjs:latest
    container_name: my-app
    build: .
    ports:
      - 3000:80
    volumes:
      - ./public/env.js:/usr/share/nginx/html/public/env.js:ro
